[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["mvn -q -DskipTests package"]

[start]
cmd = "java -Xmx512m -Xms256m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -jar target/linear-clickup-sync-1.0.0.jar"
